import 'package:flutter/material.dart';

class LearnCounter extends StatelessWidget {
  const LearnCounter({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('LearnCounter'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
